# macOS
.DS_Store
**/.DS_Store

# secrets / 部署机密（绝不入库）
.env
.env.*
!.env.example
*.key
*.pem
*-cert.pem
caddy-root.crt
**/config.local.*
*.local.md

# 依赖 / 构建产物
node_modules/
dist/
unpackage/
.cloudbase/

# 历史导入数据（真实宠物名，随函数部署到私有云端，不入公开仓）
cloudfunctions/importNotion/data.json

# 设计源图留底（宣纸纹理 base64 已内联进 App.vue，源图不进小程序包/公开仓；生成法见 worklog memory）
design-assets/
