{
  "bsonType": "object",
  "required": ["family_id", "user_id", "joined_at"],
  "permission": {
    "read": false,
    "create": false,
    "update": false,
    "delete": false
  },
  "properties": {
    "_id": {
      "description": "ID，系统自动生成"
    },
    "family_id": {
      "bsonType": "string",
      "description": "关联 family._id"
    },
    "user_id": {
      "bsonType": "string",
      "description": "用户 ID（uni-id）"
    },
    "joined_at": {
      "bsonType": "int",
      "description": "加入时间，时间戳（毫秒）"
    }
  }
}
