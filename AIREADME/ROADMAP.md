# ROADMAP — PetsLog
<!-- 节奏。不放可执行 TODO 颗粒(→项目 TODO 系统)，只放方向。 -->

## Now（当前焦点 · 2026-06-09）
「家庭 + 用户」多租户改造三轮 + 个人中心已全部落地并真机验证（ADR-008/009）：隔离键 openid → family，每云函数 assertMember 守卫；个人中心（个人档案昵称头像 / 家庭切换·管理·邀请码 / 用户·隐私协议）。全代码库经对抗式评审，修掉 user `_openid` 隔离键漏写等一批真问题。随后做底部导航重构（4 tab + 中央凸起「＋」全局录入键，提醒 / 药品合并「健康」，录入迁独立页，自定义 tabBar，ADR-010）、家庭成员昵称 / 头像读时关联 users 修复、构建瘦身（产物不再拷 node_modules，部署改云端安装依赖，根治 DevTools 刷新风暴）。随后做字段扩展轮1（records 加就诊医院 / 费用 / 病程 tag、pets 加到家日期 / 备注 / 头像、event_type 增驱虫第 7 桶、时间线病程简版筛选 + 新字段展示、parseRecord 同步抽取，ADR-012/013），经多 agent 对抗式评审修 numOrNull 假 0、tag 落库 trim、tabBar 回「宠物」被吞 等。当前 8 云函数 + 9 集合，版本 0.3.1。
此前已落地（MVP + 增强 + UI）：自然语言录入 → 解析（record / med_stock / reminder 三分流）→ 时间线 / 卡片 / 药品 / 提醒；宠物档案编辑、体重曲线、截图给兽医；「温暖治愈」UI 设计系统（ADR-007）。开源收口完成（MIT public）。

## Next（MVP 内测完善 · 1-2 个月）
- **附件 + records 扩展 + 历史数据导入**（已定型，见 ADR-011/012/013 + SPEC 数据字典）：分 3 轮：轮1 字段（**已落地 v0.3.1**：records hospital/cost/tag、pets home_date/note/avatar、event_type 驱虫第 7 桶、病程 tag 简版筛选、parseRecord 抽取）→ **轮2 附件（下一轮）**（云存储上传 / 压缩缩略图 / 记录详情页 / 家庭存储 1GB + 200MB·天限额 / 级联清理）→ 轮3 Notion 历史数据结构化导入。主粮 foods 模块模型已记、建设排此之后。
- **轮1 评审延后项（backlog）**：① 换头像 / 删宠物时级联删旧云存储 fileID（孤儿文件清理，随轮2 ADR-011 附件级联一并做；pet 头像与 user 头像同源同此缺陷）；② 给兽医小结正文已改用结构化 `desc` 拼接（不用 raw 原话、不含费用 / 就诊医院；跨医院 / 医生敏感，属主人私域，仅留时间线；records 加 desc 字段，composeVetBody / paintVet 注释钉死）；③ 病程完整视图（当前简版筛选受时间线 50 条上限截断，看不全早于 50 条的同病程记录，ADR-012 已留「完整视图后续」）。
- 邀请身边多宠家庭朋友内测，收集真实语料。
- AI 解析准确率调优（目标 ≥ 90%）：扩充 prompt few-shot、跑实测集。
- 提醒真推送：申请微信订阅消息模板 + 定时触发器，到期主动 push（见 ADR-006）。
- 测试 token 轮换为正式 token（隔离用量）。
- 真机回归两处 canvas（体重曲线 / 兽医小结图）渲染与导出。

## Later
- 一键医生病历总结（AI 汇总病史）/ 多设备云同步 / 宠物头像照片上传。
- 商业化：企业主体迁移 + 微信支付 + 会员订阅。
- 全平台扩展（Android / iOS，uni-app 同套代码）。
- 小红书 / 抖音推广。

## 已搁置（+原因）
- **自建云服务器后端**：搁置。无可用 ICP 备案域名（小程序强制要求）+ 无非自建不可的硬诉求 → 选微信云开发。见 DECISIONS ADR-002。
- **微信机器人 + Notion**：否决。封号风险 + Notion 海外不稳 + 无法商业化 / 多租户。见 DECISIONS ADR-001。
