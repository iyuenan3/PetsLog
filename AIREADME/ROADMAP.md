# ROADMAP — PetsLog
<!-- 节奏。不放可执行 TODO 颗粒(→项目 TODO 系统)，只放方向。 -->

## Now（当前焦点 · 2026-06-12 · 内测体验版）
**版本 0.4.5，11 云函数 + 11 集合，真机内测中（体验版）。** 当前焦点 = 录入重做收口 + 内测回归：录入新增结构化直填（不调 LLM，直接 saveRecord）+ 确认卡片改全字段可编辑（AI 错字段内联改不重调）+ 点＋弹「记录」三入口卡（自然语言主入口 / 结构化 / 语音占位）+ records.time 精确到分、created_at 由事件时间派生（ADR-017/018）。云函数已部署、产物已出体验版包；用户隐私保护指引已提交。**待办**：内测真机回归（录入三路 / 时间到分 / 解析结果可编辑）→ commit 前对抗式评审 → 提交 v0.4.5（前端 + 云函数源码尚未 commit）→ 加体验成员 + 发码邀友。
此前里程碑（倒序，详见 CHANGELOG）：轮4 录入防错别字（意图驱动建档）+ 头像 emoji + 手动建宠（v0.4.2，ADR-015）；LLM 切火山方舟直连（v0.4.4，ADR-016）；体重曲线交互重做（v0.4.3）；轮3 Notion 历史导入执行收口（v0.4.0/0.4.1，库内终态 9 宠 / 217 记录 / 12 主粮 / 46 附件，stats 全绿）+ 身价 / 简介 / foods 主粮模块（ADR-014）；轮2 附件（v0.3.2，ADR-011）；轮1 字段扩展（v0.3.1，ADR-012/013）；「家庭 + 用户」多租户三轮 + 个人中心（ADR-008/009，隔离键 openid → family，每云函数 assertMember 守卫）；底部导航重构（4 tab + 中央＋，自定义 tabBar，ADR-010）+ 构建瘦身（产物不拷 node_modules，云端装依赖，根治 DevTools 刷新风暴）。各轮均经多 agent 对抗式评审 + 真机验。
此前已落地（MVP + 增强 + UI）：自然语言录入 → 解析（record / med_stock / reminder 三分流）→ 时间线 / 卡片 / 药品 / 提醒；宠物档案编辑、体重曲线、截图给兽医；「温暖治愈」UI 设计系统（ADR-007）。开源收口完成（MIT public）。

## Next（MVP 内测完善 · 1-2 个月）
- **附件 + records 扩展 + 历史数据导入**（已定型，见 ADR-011/012/013/014 + SPEC 数据字典）：分 3 轮：轮1 字段（**已落地 v0.3.1**）→ 轮2 附件（**已落地 v0.3.2**）→ 轮3 Notion 历史导入 + 字段扩展（身价 / 简介 / 绝育）+ foods 主粮模块（**已落地 v0.4.0/0.4.1**，**导入已执行完毕**：库内终态 9 宠 / 217 记录 / 12 主粮 / 46 附件，经 Notion MCP 实时全量核对 + fix_times 清洗，stats 全绿）。
- **录入重做 + 内测发布**（**已落地 v0.4.5**，ADR-017/018）：结构化直填 + 确认卡片可编辑 + 点＋三入口卡 + 时间精确到分；云函数已部署、体验版包已出、隐私指引已提交。待内测真机回归 + commit。
- **家庭多租户隔离**（**已审计 + 自动化回归，2026-06-14**）：13-agent workflow 审 11 云函数 + 前端，四条隔离不变式全绿、0 可利用缺陷；新增 `tests/isolation.e2e.test.js`（64 断言 9 组：鉴权闸 / IDOR / parse 篡改链 / importNotion 越权 / 角色身份 / 邀请码踢后失效 / 同名不串档 / 退出解散）接入 `npm test`，CONVENTIONS 立 assertMember 多副本同步红线。发码邀友前的多用户隔离底已夯实（真机仍验改包伪造 family_id 的真实请求）。
- **语音录入**（下一轮，ADR-017 留口）：同声传译插件 → 转文字 → 走解析；隐私指引录音 scope.record 做时同步加。
- **评审延后项（backlog）**：storage_bytes 计数器定期校准（与真实用量并发漂移的兜底，ADR-011 tradeoff，量大再做）。（病程完整视图已转 ADR-019 落地，见下。）
- 邀请身边多宠家庭朋友内测，收集真实语料。
- 提醒：当前站内提醒（健康页 + 角标）已满足需求；微信订阅消息「真推送」暂缓，后续按需再做（见 ADR-006）。
- **病程完整视图**（ADR-019，**代码已落 / clean_tags 已执行（107 条非病程 tag 置空、幂等收口）/ 待真机验**）：tag 治理 clean_tags 幂等动作 + course.vue 独立页（概览聚合 + 跨宠下钻 + 静态体重曲线）+ timeline `list_tags`/`course` 两 action + 时间线入口条（list_tags 全集修老病程无入口）+ 记录内联 🏷️ 入口。**待补**：入口②宠物档案该宠病程（pet.vue，ADR-019 列了未做）。
- **AI 解析准确率调优（目标 ≥ 90%）**（方案已定，见 ADR-020）：raw 服务端逐字落库 + 输入输出收紧（tag 候选 / kind few-shot）+ 评测先行（混合诚实 / 端到端）。代码待落地。
- **宠物档案卡（可分享图片 + 首页轮播门面）**（**代码已落 / 待真机验**，ADR-021/022）：温暖治愈风海报卡（头像 / 名 / 物种品种 / 年龄 / 最新体重 / 陪伴天数 / 简介），离屏 canvas 出图存相册 / 长按转发，复用兽医小结导出管线；萌宠 + 轻健康，绝不含费用 / 医院 / 病史 / 病程 / 用药；零新增云函数 / 集合 / 字段。**改版 A·精致留白**（头像光晕 + 物种 chip + 图标胶囊 + 自适应年龄字号 + 轻点缀）+ 渲染抽共享模块 `src/petCard.js`。**首页（宠物 tab）主视图重做成该卡的左右滑轮播**（替换原 2 列网格，一屏一宠 + 邻卡露边 + 末张添加 + 指示点 + 点卡进详情；一块离屏 canvas 顺序出图 + 签名缓存）。社交分享物，呼应推广。待真机验。
- 真机回归：兽医小结图导出（体重曲线已于 v0.4.3 重做并真机验）。

## Later
- 一键医生病历总结（AI 汇总病史）/ 多设备云同步 / 宠物头像照片上传。
- 商业化：企业主体迁移 + 微信支付 + 会员订阅。
- 全平台扩展（Android / iOS，uni-app 同套代码）。
- 小红书 / 抖音推广。

## 已搁置（+原因）
- **自建云服务器后端**：搁置。无可用 ICP 备案域名（小程序强制要求）+ 无非自建不可的硬诉求 → 选微信云开发。见 DECISIONS ADR-002。
- **微信机器人 + Notion**：否决。封号风险 + Notion 海外不稳 + 无法商业化 / 多租户。见 DECISIONS ADR-001。
