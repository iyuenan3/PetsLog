# RELATIONS — PetsLog
<!-- 生态连接。共享底座写在属主项目，这里只指向属主。仓库公开 → 私有项目坐标抽象化。 -->

## 出向依赖（我用了谁）
| 依赖 | 用途 | 备注 |
|---|---|---|
| 自建 LLM 网关 | 云函数 POST `/v1/chat/completions` 做自然语言 → JSON 结构化（OpenAI 兼容文本模型）| 私有基础设施项目，endpoint/CA/token 不入库（见 DEPLOYMENT）|
| 微信云开发 | Serverless 后端（云函数 + 云数据库 + 云存储 + 自带登录态）| 第三方平台 |

## 入向（谁用我）
- 个人作品集页（展示用，best-effort）：非代码依赖。

## 共享底座 / 复用资产
- **自建 LLM 网关的自签根证书（依赖约束）**：PetsLog 云函数需信任该网关的自签 root CA 才能调用。属主为开发者的私有 LLM 网关项目；该 CA 被多个私有客户端 pin，属主侧约束为**不可轮换 / 重置**，变更则本项目 AI 记录同步中断 → CA 调整须联动本项目。本项目为新增消费方。
- PetsLog 不拥有任何被他人复用的共享底座。
